Linked List Kata

This program contains following operations on linked list with TDD (Google Test)

Adding, deleting, inserting, and searching a node
Reversing the linked list 
Copying the linked list

Instruction to run the test cases:

1.cmake CMakeLists.txt 
2.make
3../runTests
